/* export type RootStackParamListbb = {
    ProductDetailsNm: { id: string };
    HomeScreenNm: { };          // Though we are not passing any params to it but we need to define it here ...otherwise typescript gives errorx `Type '"HomeScreenNm"' is not assignable to type '"ProductDetailsNm"'. in the `srcy\routery\HomeStacky\index.tsx`own explore:
    // add other screens here
  };
   */